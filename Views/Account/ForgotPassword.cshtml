@model vitacure.Models.ViewModels.Auth.ForgotPasswordViewModel

@section Styles {
    <link rel="stylesheet" href="~/css/pages/auth.css" asp-append-version="true" />
}

<div class="auth-page">
    <div class="container auth-container">
        <div class="auth-card">
            <div class="auth-eyebrow">Şifre Sıfırlama</div>
            <h1 class="auth-title">Yeni şifre bağlantısı oluştur</h1>
            <p class="auth-subtitle">Kayıtlı müşteri e-postanızı girin. E-posta servisi hazır olana kadar bağlantıyı bu ekranda göstereceğiz.</p>

            <form method="post" class="auth-form">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-4">
                    <label asp-for="Email" class="form-label"></label>
                    <input asp-for="Email" class="form-control auth-input" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>

                <button type="submit" class="btn auth-submit-btn">Bağlantı Oluştur</button>
            </form>

            @if (!string.IsNullOrWhiteSpace(Model.Message))
            {
                <div class="alert alert-info mt-4">@Model.Message</div>
            }

            @if (!string.IsNullOrWhiteSpace(Model.ResetUrl))
            {
                <div class="auth-footer-link mt-3">
                    Şifreyi sıfırla: <a href="@Model.ResetUrl">Bağlantıyı aç</a>
                </div>
            }

            <div class="auth-footer-link">
                Giriş ekranına dönmek için <a href="/login">buraya tıkla</a>.
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
