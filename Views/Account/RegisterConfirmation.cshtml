@model vitacure.Models.ViewModels.Auth.RegisterConfirmationViewModel

@section Styles {
    <link rel="stylesheet" href="~/css/pages/auth.css" asp-append-version="true" />
}

<div class="auth-page">
    <div class="container auth-container">
        <div class="auth-card">
            <div class="auth-eyebrow">E-posta Dogrulama</div>
            <h1 class="auth-title">Hesabin olusturuldu</h1>
            <p class="auth-subtitle">@Model.Email adresi i�in dogrulama gerekiyor.</p>

            <div class="alert alert-info">@Model.Message</div>

            @if (!string.IsNullOrWhiteSpace(Model.ConfirmationUrl))
            {
                <div class="auth-footer-link mt-3">
                    Hesabi dogrula: <a href="@Model.ConfirmationUrl">Baglantiyi a�</a>
                </div>
            }

            <div class="auth-footer-link mt-3">
                Dogrulama sonrasi <a href="/login">giris ekranina d�nebilirsin</a>.
            </div>
        </div>
    </div>
</div>
